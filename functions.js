//function example(x) {
// return x * 2;
//}
//console.log(example(6));

function eat(food) {
  // function defined with the variable eat; with the argument food //
  return food + " tasted really good."; // will output following string along with the argument //
}
console.log(eat("bananas")); // calling the function to print bananas as the argument, along with the remainder of the function's output //
